name: Build PDF and deploy to GitHub Pages

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

# Only allow one deployment at a time
concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  pages: write
  id-token: write
  pull-requests: write

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      BUILD_IMAGE: ghcr.io/stamped-principles/build-latex:latest
    steps:
      - uses: actions/checkout@v4

      - name: Pull build container
        # Fall back to building the image locally so PRs that change the
        # container still build before the image is published to GHCR.
        run: |
          docker pull "$BUILD_IMAGE" || \
            docker build -t "$BUILD_IMAGE" -f containers/build-latex.Dockerfile containers

      - name: Build PDF
        run: |
          docker run --rm --user "$(id -u):$(id -g)" -e HOME=/tmp \
            -v "$PWD:/work" -w /work "$BUILD_IMAGE" make

      - name: Prepare Pages artifact
        run: |
          mkdir -p _site
          cp main.pdf _site/
          cp .github/pages/index.html _site/

      - name: Upload Pages artifact
        if: github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

      - name: Generate PDF diff against main
        if: github.event_name == 'pull_request'
        id: diff
        run: |
          docker run --rm -v "$PWD:/work" ghcr.io/stamped-principles/diff-pdf:latest \
            bash ./code/gh-pages-diff-pdf.sh \
              main.pdf \
              "https://stamped-principles.github.io/stamped-paper/main.pdf" \
              "gh-pages-pr-${{ github.event.pull_request.number }}" \
              "${{ github.repository }}" \
              "${{ github.server_url }}" \
              "${{ github.event.pull_request.head.sha }}"
          # Pass comment body to next step
          {
            echo 'body<<ENDOFCOMMENT'
            cat pr-comment.md
            echo 'ENDOFCOMMENT'
          } >> "$GITHUB_OUTPUT"

      - name: Push PDF and diffs to PR preview branch
        if: github.event_name == 'pull_request'
        env:
          PR_NUMBER: ${{ github.event.pull_request.number }}
          HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          BRANCH="gh-pages-pr-${PR_NUMBER}"
          git checkout --orphan "$BRANCH"
          git rm -rf --cached .
          git add -f main.pdf
          # Add diff PDF and per-page diff images if generated
          [ -f diff.pdf ] && git add -f diff.pdf
          for f in diff-page-*.png; do
            [ -f "$f" ] && git add -f "$f"
          done
          git commit -m "PDF preview for PR #${PR_NUMBER} (${HEAD_SHA:0:7})"
          git push -f origin "$BRANCH"

      - name: Find existing diff comment
        if: github.event_name == 'pull_request'
        uses: peter-evans/find-comment@v3
        id: find-comment
        with:
          issue-number: ${{ github.event.pull_request.number }}
          comment-author: 'github-actions[bot]'
          body-includes: '<!-- pdf-preview -->'

      - name: Comment on PR with PDF link and diff
        if: github.event_name == 'pull_request'
        uses: peter-evans/create-or-update-comment@v4
        with:
          comment-id: ${{ steps.find-comment.outputs.comment-id }}
          issue-number: ${{ github.event.pull_request.number }}
          body: ${{ steps.diff.outputs.body }}
          edit-mode: replace

  deploy:
    if: github.ref == 'refs/heads/main'
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
